# DevMentor AI - Environment Configuration Example
# Copy this file to .env and configure your settings

# =============================================================================
# REQUIRED: CORS Configuration
# =============================================================================

# Comma-separated list of allowed origins (REQUIRED)
# Examples:
# ALLOWED_ORIGINS=https://localhost:3000,https://devmentor-ai.com,https://app.devmentor-ai.com
# ALLOWED_ORIGINS=https://localhost:8080,https://localhost:3000
# ALLOWED_ORIGINS=https://yourdomain.com

ALLOWED_ORIGINS=https://localhost:8080,https://localhost:3000,https://devmentor-ai.com

# Allow server-to-server requests without Origin header (optional)
# Set to 'true' only if you need to allow API calls from servers
# Default: false (blocks requests without Origin header)
ALLOW_SERVER_TO_SERVER=false

# =============================================================================
# API Keys (Optional - can be configured via API endpoints)
# =============================================================================

# OpenAI API Key
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key  
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Google AI API Key
GOOGLE_API_KEY=your_google_api_key_here

# =============================================================================
# Server Configuration
# =============================================================================

# Server port (default: 3001)
PORT=3001

# Environment (development, production)
NODE_ENV=development

# =============================================================================
# Security Configuration
# =============================================================================

# Rate limiting (requests per 15 minutes per IP)
RATE_LIMIT_MAX=100

# Request timeout (milliseconds)
REQUEST_TIMEOUT=30000

# =============================================================================
# Logging Configuration
# =============================================================================

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# Enable request logging
ENABLE_REQUEST_LOGGING=true

# =============================================================================
# CORS Security Notes
# =============================================================================

# 1. ALLOWED_ORIGINS is REQUIRED - the server will exit if not set
# 2. Use exact origins only - wildcards (*) are not compatible with credentials
# 3. Include protocol (https://) in origins
# 4. Separate multiple origins with commas
# 5. No spaces around commas
# 6. Empty origins are filtered out automatically

# Examples of VALID configurations:
# ALLOWED_ORIGINS=https://localhost:3000
# ALLOWED_ORIGINS=https://localhost:3000,https://localhost:8080
# ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com

# Examples of INVALID configurations:
# ALLOWED_ORIGINS=https://*.example.com  # Wildcards not allowed
# ALLOWED_ORIGINS=localhost:3000        # Missing protocol
# ALLOWED_ORIGINS=https://localhost:3000, https://localhost:8080  # Spaces around comma
# ALLOWED_ORIGINS=                      # Empty (will cause server to exit)