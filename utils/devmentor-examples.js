/**
 * DevMentor AI - Usage Examples
 * Exemplos práticos de como usar o sistema completo
 */

// ========================================
// EXEMPLO 1: CONFIGURAÇÃO BÁSICA
// ========================================

async function exemploConfiguracaoBasica() {
  console.log('🔧 === EXEMPLO 1: CONFIGURAÇÃO BÁSICA ===');
  
  // Configurar sistema
  window.devMentorConfig.configure({
    debug: true,
    showNotifications: true,
    geminiApiKey: 'sua-api-key-aqui', // Opcional
    features: {
      citations: true,
      playground: true,
      metaphors: true,
      diagrams: true,
      videos: true,
      quizzes: true
    }
  });
  
  // Inicializar sistema
  await window.DevMentorHelpers.initializeMediaRichSystem();
  
  console.log('✅ Sistema configurado e inicializado!');
}

// ========================================
// EXEMPLO 2: GERAR EXPLICAÇÃO RICA
// ========================================

async function exemploExplicacaoRica() {
  console.log('📖 === EXEMPLO 2: EXPLICAÇÃO RICA ===');
  
  const codigo = `
async function fetchUserData(userId) {
  try {
    const response = await fetch(\`/api/users/\${userId}\`);
    const data = await response.json();
    return data;
  } catch (error) {
    console.error('Erro:', error);
    throw error;
  }
}
  `;
  
  // Gerar explicação rica completa
  const explicacao = await window.DevMentorHelpers.generateRichExplanation(
    codigo,
    'complexity',        // Tipo de análise
    'javascript',        // Linguagem
    'intermediate'       // Nível do usuário
  );
  
  console.log('📊 Explicação gerada:', explicacao.length, 'caracteres');
  console.log('🎬 Inclui vídeo:', explicacao.includes('video'));
  console.log('📚 Inclui citações:', explicacao.includes('citations'));
  console.log('🎮 Inclui playground:', explicacao.includes('playground'));
  console.log('🎨 Inclui metáforas:', explicacao.includes('metaphor'));
  console.log('📊 Inclui diagramas:', explicacao.includes('mermaid'));
  console.log('✅ Inclui quizzes:', explicacao.includes('quiz'));
}

// ========================================
// EXEMPLO 3: USAR COMPONENTES INDIVIDUAIS
// ========================================

async function exemploComponentesIndividuais() {
  console.log('🧩 === EXEMPLO 3: COMPONENTES INDIVIDUAIS ===');
  
  // 1. CitationEngine
  if (window.citationEngine) {
    const conceitos = [
      { name: 'async/await', confidence: 0.9 },
      { name: 'promises', confidence: 0.8 }
    ];
    
    const citacoes = await window.citationEngine.findCitations(conceitos, 'javascript');
    console.log('📚 Citações encontradas:', Object.keys(citacoes).length, 'categorias');
  }
  
  // 2. VisualMetaphorEngine
  if (window.visualMetaphorEngine) {
    const metafora = window.visualMetaphorEngine.getMetaphor('async_await');
    console.log('🎨 Metáfora:', metafora?.metaphor);
  }
  
  // 3. DiagramGenerator
  if (window.diagramGenerator) {
    const codigo = 'if (condition) { doSomething(); } else { doSomethingElse(); }';
    const diagrama = await window.diagramGenerator.autoGenerateDiagram(codigo, 'complexity');
    console.log('📊 Diagrama gerado:', diagrama?.type);
  }
  
  // 4. QuizGenerator
  if (window.quizGenerator) {
    const conceitos = [{ name: 'async/await', confidence: 0.9 }];
    const quizzes = await window.quizGenerator.generateQuizzes(conceitos, 'intermediate');
    console.log('✅ Quizzes gerados:', quizzes.length);
  }
}

// ========================================
// EXEMPLO 4: INTEGRAÇÃO COM GEMINI PRO
// ========================================

async function exemploGeminiPro() {
  console.log('🤖 === EXEMPLO 4: GEMINI PRO ===');
  
  if (!window.geminiProIntegration) {
    console.log('⚠️ Gemini Pro não disponível');
    return;
  }
  
  // Configurar Gemini Pro
  window.geminiProIntegration.configure('sua-api-key-aqui');
  
  const codigo = 'const data = await fetch("/api");';
  
  try {
    // Explicação inteligente
    const explicacao = await window.geminiProIntegration.generateIntelligentExplanation(
      codigo,
      'javascript',
      'complexity',
      'intermediate'
    );
    console.log('🧠 Explicação inteligente:', explicacao.mainConcept);
    
    // Roteiro de vídeo
    const roteiro = await window.geminiProIntegration.generateVideoScript(
      codigo,
      'javascript',
      'optimization',
      45
    );
    console.log('🎬 Roteiro gerado:', roteiro.scenes?.length, 'cenas');
    
    // Quizzes personalizados
    const quizzes = await window.geminiProIntegration.generatePersonalizedQuizzes(
      codigo,
      'javascript',
      [{ name: 'async/await', confidence: 0.9 }],
      'intermediate'
    );
    console.log('✅ Quizzes personalizados:', quizzes.length);
    
  } catch (error) {
    console.warn('⚠️ Erro no Gemini Pro:', error.message);
  }
}

// ========================================
// EXEMPLO 5: PLAYGROUND INTERATIVO
// ========================================

function exemploPlayground() {
  console.log('🎮 === EXEMPLO 5: PLAYGROUND ===');
  
  if (!window.interactivePlayground) {
    console.log('⚠️ Playground não disponível');
    return;
  }
  
  const codigoInicial = `
// Exemplo de JavaScript
function calcularFatorial(n) {
  if (n <= 1) return 1;
  return n * calcularFatorial(n - 1);
}

console.log(calcularFatorial(5));
  `;
  
  // Criar playground
  const playgroundHTML = window.interactivePlayground.createPlayground(
    codigoInicial,
    'javascript',
    {
      showVisualizer: true,
      showConsole: true,
      showControls: true
    }
  );
  
  console.log('🎮 Playground criado:', playgroundHTML.length, 'caracteres');
  
  // Inserir no DOM (exemplo)
  const container = document.getElementById('playground-container');
  if (container) {
    container.innerHTML = playgroundHTML;
  }
}

// ========================================
// EXEMPLO 6: DEMO COMPLETA
// ========================================

async function exemploDemoCompleta() {
  console.log('🎬 === EXEMPLO 6: DEMO COMPLETA ===');
  
  if (!window.completeMediaRichDemo) {
    console.log('⚠️ Demo não disponível');
    return;
  }
  
  const demo = new window.CompleteMediaRichDemo();
  
  // Demo rápida
  await demo.runQuickDemo();
  
  // Demo completa (comentado para não ser muito verboso)
  // await demo.runCompleteDemo();
  
  // Demo interativa com interface
  await demo.runInteractiveDemo();
}

// ========================================
// EXEMPLO 7: MONITORAMENTO E STATUS
// ========================================

function exemploMonitoramento() {
  console.log('📊 === EXEMPLO 7: MONITORAMENTO ===');
  
  // Status do sistema
  const status = window.DevMentorHelpers.getMediaRichSystemStatus();
  console.log('📊 Status do sistema:', status);
  
  // Configuração atual
  const config = window.devMentorConfig.getConfig();
  console.log('⚙️ Configuração:', config);
  
  // Status Gemini Pro
  if (window.geminiProIntegration) {
    const geminiStatus = window.geminiProIntegration.getStatus();
    console.log('🤖 Status Gemini Pro:', geminiStatus);
  }
}

// ========================================
// EXEMPLO 8: PERSONALIZAÇÃO AVANÇADA
// ========================================

function exemploPersonalizacao() {
  console.log('🎨 === EXEMPLO 8: PERSONALIZAÇÃO ===');
  
  // Personalizar configuração
  window.devMentorConfig.updateConfig({
    theme: 'dark',
    language: 'en-US',
    features: {
      citations: true,
      playground: true,
      metaphors: false,  // Desabilitar metáforas
      diagrams: true,
      videos: false,     // Desabilitar vídeos
      quizzes: true
    }
  });
  
  // Personalizar estilos CSS
  const customStyles = `
    .devmentor-explanation {
      font-family: 'Monaco', monospace;
      background: #1e1e1e;
      color: #d4d4d4;
    }
    
    .devmentor-citation {
      border-left: 4px solid #007acc;
      background: #2d2d30;
    }
  `;
  
  // Injetar estilos personalizados
  const styleSheet = document.createElement('style');
  styleSheet.textContent = customStyles;
  document.head.appendChild(styleSheet);
  
  console.log('🎨 Personalização aplicada!');
}

// ========================================
// EXEMPLO 9: INTEGRAÇÃO COM APLICAÇÃO
// ========================================

class MinhaAplicacao {
  constructor() {
    this.devMentorConfig = {
      debug: false,
      showNotifications: true,
      features: {
        citations: true,
        playground: true,
        metaphors: true,
        diagrams: true,
        videos: true,
        quizzes: true
      }
    };
  }
  
  async inicializar() {
    // Configurar DevMentor AI
    window.devMentorConfig.configure(this.devMentorConfig);
    
    // Inicializar sistema
    await window.DevMentorHelpers.initializeMediaRichSystem();
    
    console.log('✅ Aplicação inicializada com DevMentor AI!');
  }
  
  async explicarCodigo(codigo, linguagem, tipoAnalise) {
    try {
      const explicacao = await window.DevMentorHelpers.generateRichExplanation(
        codigo,
        tipoAnalise,
        linguagem,
        'intermediate'
      );
      
      return explicacao;
    } catch (error) {
      console.error('Erro ao gerar explicação:', error);
      return 'Erro ao gerar explicação';
    }
  }
  
  async criarPlayground(codigo, linguagem) {
    if (!window.interactivePlayground) {
      return null;
    }
    
    return window.interactivePlayground.createPlayground(
      codigo,
      linguagem,
      { showVisualizer: true }
    );
  }
}

// ========================================
// EXEMPLO 10: TESTES E VALIDAÇÃO
// ========================================

async function exemploTestes() {
  console.log('🧪 === EXEMPLO 10: TESTES ===');
  
  const testes = [
    {
      nome: 'Inicialização do sistema',
      teste: async () => {
        const status = window.DevMentorHelpers.getMediaRichSystemStatus();
        return status.allComponentsLoaded;
      }
    },
    {
      nome: 'Geração de explicação',
      teste: async () => {
        const explicacao = await window.DevMentorHelpers.generateRichExplanation(
          'console.log("test");',
          'complexity',
          'javascript',
          'beginner'
        );
        return explicacao.length > 0;
      }
    },
    {
      nome: 'CitationEngine',
      teste: async () => {
        if (!window.citationEngine) return false;
        const citacoes = await window.citationEngine.findCitations(
          [{ name: 'javascript', confidence: 0.8 }],
          'javascript'
        );
        return Object.keys(citacoes).length > 0;
      }
    },
    {
      nome: 'VisualMetaphorEngine',
      teste: async () => {
        if (!window.visualMetaphorEngine) return false;
        const metafora = window.visualMetaphorEngine.getMetaphor('async_await');
        return metafora && metafora.metaphor;
      }
    },
    {
      nome: 'DiagramGenerator',
      teste: async () => {
        if (!window.diagramGenerator) return false;
        const diagrama = await window.diagramGenerator.autoGenerateDiagram(
          'if (true) { console.log("test"); }',
          'complexity'
        );
        return diagrama && diagrama.type;
      }
    }
  ];
  
  console.log('🧪 Executando testes...');
  
  for (const teste of testes) {
    try {
      const resultado = await teste.teste();
      console.log(`${resultado ? '✅' : '❌'} ${teste.nome}`);
    } catch (error) {
      console.log(`❌ ${teste.nome} - Erro:`, error.message);
    }
  }
  
  console.log('🧪 Testes concluídos!');
}

// ========================================
// EXECUTAR TODOS OS EXEMPLOS
// ========================================

async function executarTodosExemplos() {
  console.log('🚀 === EXECUTANDO TODOS OS EXEMPLOS ===\n');
  
  try {
    await exemploConfiguracaoBasica();
    console.log('');
    
    await exemploExplicacaoRica();
    console.log('');
    
    await exemploComponentesIndividuais();
    console.log('');
    
    await exemploGeminiPro();
    console.log('');
    
    exemploPlayground();
    console.log('');
    
    await exemploDemoCompleta();
    console.log('');
    
    exemploMonitoramento();
    console.log('');
    
    exemploPersonalizacao();
    console.log('');
    
    await exemploTestes();
    console.log('');
    
    console.log('🎉 Todos os exemplos executados com sucesso!');
    
  } catch (error) {
    console.error('❌ Erro durante execução dos exemplos:', error);
  }
}

// Exportar exemplos para uso global
window.DevMentorExamples = {
  configuracaoBasica: exemploConfiguracaoBasica,
  explicacaoRica: exemploExplicacaoRica,
  componentesIndividuais: exemploComponentesIndividuais,
  geminiPro: exemploGeminiPro,
  playground: exemploPlayground,
  demoCompleta: exemploDemoCompleta,
  monitoramento: exemploMonitoramento,
  personalizacao: exemploPersonalizacao,
  testes: exemploTestes,
  executarTodos: executarTodosExemplos
};

console.log('📚 DevMentor Examples carregados!');


























